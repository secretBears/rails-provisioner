harald_docker_images
====================

A collection of Dockerfiles for compiling harald sites...

To add a docker image from one of the files, go to the directory and run:
```sh
sudo docker build -t localhost:5000/[name] .
```


